# Secrets — never commit service-account keys or env files
.env
*serviceAccountKey*.json
*firebase-adminsdk*.json

# Build artifacts
/bin/
/server
/seed

# Go
*.test
*.out

# Local compose override (not for VPS)
docker-compose.override.yml
.vercel
